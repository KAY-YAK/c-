#include"SlotMachine.h"
int main(){
  SlotMachine slot;
  // slot.make_shapes();
  // cout << "HABLA HABLE"<<endl;
  // cout<<(slot.reel[0])->draw()<<endl;
  // cout<<(slot.reel[1])->draw()<<endl;
  // cout<<(slot.reel[2])->draw()<<endl;
  // slot.display_shapes();
  // slot.release_shapes();
  // slot.make_shapes();
  // cout << "HABLA HABLE_1"<<endl;
  // cout<<(slot.reel[0])->draw()<<endl;
  // cout<<(slot.reel[1])->draw()<<endl;
  // cout<<(slot.reel[2])->draw()<<endl;
  // //delete [] slot.reel;
  // slot.display_shapes();
  slot.run();
}
